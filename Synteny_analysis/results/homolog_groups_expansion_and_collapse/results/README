Note: due to file limits, directories were compressed into .tar.gz files. In some cases intermediate files are abscent, but they are reproducible by following the respective notebook that generates them.
